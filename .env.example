# Klipper/Moonraker Configuration
# Copy this file to .env and update with your actual settings
# GingerView uses nginx proxy for Moonraker connections (no IP hardcoding)
# The JavaScript code will use relative paths via window.location

# Moonraker connection path (relative path via nginx proxy)
VITE_MOONRAKER_PATH=/moonraker

# Optional: Printer name for display
VITE_PRINTER_NAME=Klipper Printer

# Optional: Connection timeout in milliseconds
VITE_CONNECTION_TIMEOUT=5000
