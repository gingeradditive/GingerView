node_modules

# package-lock.json is deliberately NOT ignored: it is tracked, and CI runs
# `npm ci`, which fails without it.

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
